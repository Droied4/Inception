# syntax=docker/dockerfile:1
ARG BASE_IMAGE
ARG BASE_DIGEST
FROM ${BASE_IMAGE}@sha256:${BASE_DIGEST}

ARG MARIADB_PORT
EXPOSE ${MARIADB_PORT}

RUN 	apk update && \
	apk add --no-cache mariadb

CMD ["sh", "-c", "while true; do echo 'Probando paquetes...'; sleep 5; done"]
